<h1>Categories</h2>

<table>
  <tr>
    <th>Name</th>
    <th></th>
  </tr>

  <% @categories.each do |category| %>
   <tr>
      <td><%= category.name %></td>
    </tr>
  <% end %>
</table>

<h2><%= link_to 'Add a category', new_category_path %></h2>

<p><%= link_to 'Back to main page', recipes_path %></p>